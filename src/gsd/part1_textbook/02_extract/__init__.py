"""Glycan term extraction LangGraph workflow.

Note: This package directory starts with a digit (02_extract), so it cannot
be imported via standard dotted syntax.  Run graph.py directly instead:

    python src/gsd/part1_textbook/02_extract/graph.py [--output-path PATH] [--max-documents N]
"""
